#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

#define REP(i, n) for(int i = 0; i < (int)(n); ++i)
#define FOR(i, m, n) for(int i = (int)(m); i < (int)(n); ++i)


int main()
{
    int N, Q;
    cin >> N >> Q;
    string c;
    cin >> c;
    vector<int> l(N), r(N);
    REP(i, N) cin >> l[i] >> r[i];

    

    return 0;
}